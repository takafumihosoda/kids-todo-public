name: Evening Notice

on: [workflow_dispatch]
# ※もしパパさんの元のファイルに schedule（時間指定）の行があった場合は、それも残しておいてください

jobs:
  send-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Send LINE (Stopped)
        run: echo "上限対策のため、20時のLINE自動配信をストップしています。"
